name: eyevoice
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  flutter_tts: ^4.2.5
  shared_preferences: ^2.5.5

  # Eye-tracking : détection visage/iris via caméra frontale.
  #
  # Choix revalidé en Phase 1b (eye-tracking-engineer), `mediapipe_face_mesh`
  # conservé (pas de remplacement malgré la fraîcheur du package — publié
  # < 24 h, 9 likes au moment de l'audit, verified publisher). Un candidat de
  # repli plus "mature" a été essayé en premier (`face_detection_tflite`,
  # même modèles MediaPipe via LiteRT) mais écarté : sa dépendance
  # `opencv_dart`/`dartcv4` s'appuie sur le système expérimental de "native
  # assets" de Dart (hooks + `native_toolchain_cmake`), qui tente de
  # compiler une lib native pour la plateforme HÔTE dès `flutter test` — pas
  # seulement au build mobile. Sur ce projet, ça fait échouer `flutter test`
  # pour TOUTE la codebase (pas seulement `eyetracking`) sur toute machine
  # sans CMake + toolchain C++ installés (vérifié : échec immédiat sur ce
  # poste de dev, faute de CMake/Visual Studio). Ça viole frontalement la
  # contrainte "détection/mapping/dwell testables séparément" — un risque de
  # dépendance non testable l'emporte sur un risque de fraîcheur de
  # package. `mediapipe_face_mesh` est un plugin FFI classique (binaires
  # natifs précompilés par plateforme, chargés via `dart:ffi`/`dlopen` au
  # runtime sur l'appareil) : sa résolution de dépendances est légère (juste
  # `ffi` + Flutter/test), sans hook de compilation côté hôte — vérifié via
  # `flutter pub get` sur un projet de sondage isolé. Fournit 468 points de
  # mesh + jusqu'à 478 avec landmarks d'iris (`enableIris: true`),
  # Android + iOS. À surveiller (maintenance/stabilité) au fil des prochaines
  # phases ; le contrat `FaceGazeDetector` de `lib/eyetracking/detection`
  # isole ce choix pour permettre un remplacement sans impact sur le reste
  # du pipeline si besoin.
  mediapipe_face_mesh: ^2.0.0
  # Accès au flux caméra frontale (CameraImage) consommé par
  # `mediapipe_face_mesh`. Plugin fédéré standard (pas de hook de
  # compilation côté hôte), verrouillé aux côtés du détecteur ci-dessus.
  camera: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # `menu-config.json` (SPECIFICATIONS_FONCTIONNELLES.md section 11) : la
  # configuration réelle des menus, chargée au runtime par
  # `lib/data/menu_config_repository.dart` via `rootBundle.loadString`. Le
  # contenu suit fidèlement `sample_menu_config.dart` (fixture Dart en
  # mémoire conservée pour les tests), désormais aussi disponible comme
  # vraie source de données externe au code (comblant la réserve du critère
  # 8 d'ACCEPTANCE_CHECKLIST.md).
  assets:
    - assets/menu-config.json

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
